#ifndef ${define}
#define ${define}

#include "BaseAction.H"
#include "WidgetAccess.H"

class Fl_Input;

SCLASS(${class}) : public BaseAction
{
public:
    ${class}(BaseActionWP&& parent, const WidgetAccessSP<Fl_Group> &group);
    virtual ~${class}();
    void start() override final;

    private:
    void onCancel();
    void onInput();
    void onTimeout();

    private:
    WidgetAccessSP<Fl_Group> group;
    ButtonSP cancelB;
    WidgetInActionSP<Fl_Input> nameInput;
    TimeoutInActionSP nextTimeout;


};

#endif // ${define}
